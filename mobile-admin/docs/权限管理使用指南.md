# 移动管理端权限管理使用指南

**文档版本**: v1.0.0 | **创建时间**: 2025-12-06 | **适用范围**: 移动管理端开发团队

---

## 📋 概述

移动管理端实现了基于角色的权限控制（RBAC - Role-Based Access Control），支持5个角色等级和25+个权限点，确保不同角色的用户只能访问和操作其权限范围内的功能。

---

## 🎯 角色定义

### 角色层级

| 角色 | 等级 | 标识 | 说明 |
|------|------|------|------|
| **超级管理员** | L5 | `super_admin` | 拥有所有权限，可管理整个系统 |
| **区域经理** | L4 | `regional_manager` | 管理指定区域的所有业务 |
| **门店经理** | L3 | `store_manager` | 管理指定门店的业务 |
| **现场管理员** | L2 | `field_admin` | 处理现场具体业务操作 |
| **客服人员** | L1 | `customer_service` | 查看和处理客户相关信息 |

### 角色权限矩阵

| 功能模块 | 超级管理员 | 区域经理 | 门店经理 | 现场管理员 | 客服人员 |
|----------|------------|----------|----------|------------|----------|
| **数据概览** | ✅ 查看 | ✅ 查看 | ✅ 查看 | ✅ 查看 | ✅ 查看 |
| **用户管理** | ✅ 增删改查 | ✅ 查看 | ✅ 查看 | ✅ 查看 | ✅ 查看 |
| **订单管理** | ✅ 增删改查 | ✅ 增删改查 | ✅ 增改查 | ✅ 增改查 | ✅ 查看 |
| **车辆管理** | ✅ 增删改查 | ✅ 查看 | ✅ 增改查 | ✅ 改查 | ✅ 查看 |
| **车辆调度** | ✅ 调度 | ❌ 无权 | ✅ 调度 | ✅ 调度 | ❌ 无权 |
| **财务管理** | ✅ 查看 | ✅ 查看 | ✅ 查看 | ❌ 无权 | ❌ 无权 |
| **系统设置** | ✅ 增删改查 | ❌ 无权 | ❌ 无权 | ❌ 无权 | ❌ 无权 |
| **托管管理** | ✅ 查看审核 | ✅ 查看审核 | ✅ 查看审核 | ❌ 无权 | ❌ 无权 |
| **消息工单** | ✅ 管理 | ✅ 管理 | ✅ 管理 | ✅ 管理 | ✅ 管理 |

---

## 🔧 使用方法

### 1. 在页面中使用权限组件

#### 1.1 权限按钮组件

```vue
<template>
  <view>
    <!-- 单个权限控制 -->
    <PermissionButton
      permission="create_order"
      type="primary"
      @click="handleCreateOrder"
    >
      创建订单
    </PermissionButton>

    <!-- 多个权限控制（满足任一即可） -->
    <PermissionButton
      :permissions="['update_order', 'delete_order']"
      type="error"
      @click="handleDeleteOrder"
    >
      删除订单
    </PermissionButton>

    <!-- 需要所有权限 -->
    <PermissionButton
      :permissions="['view_finance', 'manage_finance']"
      :require-all="true"
      type="warning"
      @click="handleFinance"
    >
      财务管理
    </PermissionButton>

    <!-- 无权限时显示禁用状态 -->
    <PermissionButton
      permission="dispatch_vehicle"
      :show-disabled="true"
      denied-message="您没有车辆调度权限"
      type="primary"
      @click="handleDispatch"
    >
      车辆调度
    </PermissionButton>
  </view>
</template>

<script>
import PermissionButton from '@/components/common/PermissionButton.vue'

export default {
  components: {
    PermissionButton
  },

  methods: {
    handleCreateOrder() {
      console.log('创建订单')
    },

    handleDeleteOrder() {
      console.log('删除订单')
    },

    handleFinance() {
      console.log('财务管理')
    },

    handleDispatch() {
      console.log('车辆调度')
    }
  }
}
</script>
```

#### 1.2 权限视图组件

```vue
<template>
  <view>
    <!-- 单个权限控制 -->
    <PermissionView permission="view_finance">
      <view class="finance-section">
        <text>财务数据内容</text>
      </view>
    </PermissionView>

    <!-- 多个权限控制 -->
    <PermissionView :permissions="['update_order', 'delete_order']">
      <view class="order-actions">
        <button>编辑</button>
        <button>删除</button>
      </view>
    </PermissionView>

    <!-- 显示无权限提示 -->
    <PermissionView
      permission="manage_settings"
      :show-fallback="true"
    >
      <view class="settings-section">
        <text>系统设置内容</text>
      </view>
      <template #fallback>
        <view class="no-permission">
          <text>您没有权限查看系统设置</text>
        </view>
      </template>
    </PermissionView>

    <!-- 需要所有权限 -->
    <PermissionView
      :permissions="['view_finance', 'manage_finance']"
      :require-all="true"
      :show-fallback="true"
      denied-message="您需要财务查看和管理权限"
    >
      <view class="finance-management">
        <text>财务管理内容</text>
      </view>
    </PermissionView>
  </view>
</template>

<script>
import PermissionView from '@/components/common/PermissionView.vue'

export default {
  components: {
    PermissionView
  }
}
</script>
```

### 2. 在JavaScript中使用权限工具

```javascript
import {
  hasPermission,
  hasAnyPermission,
  hasAllPermissions,
  hasRole,
  getCurrentRole,
  getRoleName,
  filterDataByPermission,
  handlePermissionDenied
} from '@/utils/permission'

export default {
  data() {
    return {
      orders: []
    }
  },

  computed: {
    // 检查是否有创建订单权限
    canCreateOrder() {
      return hasPermission('create_order')
    },

    // 检查是否有任一权限
    canManageOrder() {
      return hasAnyPermission(['update_order', 'delete_order'])
    },

    // 检查是否有所有权限
    canManageFinance() {
      return hasAllPermissions(['view_finance', 'manage_finance'])
    },

    // 检查角色
    isManager() {
      return hasRole('store_manager')
    },

    // 获取当前角色名称
    currentRoleName() {
      const role = getCurrentRole()
      return getRoleName(role)
    },

    // 根据权限过滤数据
    filteredOrders() {
      return filterDataByPermission(this.orders, 'orders')
    }
  },

  methods: {
    handleAction() {
      // 检查权限
      if (!hasPermission('update_order')) {
        handlePermissionDenied('您没有权限更新订单')
        return
      }

      // 执行操作
      this.updateOrder()
    },

    updateOrder() {
      console.log('更新订单')
    }
  }
}
```

### 3. 路由守卫使用

路由守卫已自动初始化，会在页面跳转前自动检查权限。

#### 3.1 在main.js中初始化

```javascript
import { initRouteGuard } from '@/utils/route-guard'

// 初始化路由守卫
initRouteGuard()
```

#### 3.2 在页面中使用（可选）

```javascript
import { pageGuard } from '@/utils/route-guard'

export default {
  onLoad(options) {
    // 检查页面访问权限
    if (!pageGuard(this, options)) {
      // 无权限，已自动处理跳转
      return
    }

    // 有权限，继续加载页面
    this.loadData()
  }
}
```

---

## 📝 权限列表

### 完整权限清单

| 权限标识 | 权限名称 | 说明 |
|----------|----------|------|
| `view_dashboard` | 查看工作台 | 查看工作台数据概览 |
| `view_users` | 查看用户 | 查看用户列表和详情 |
| `create_user` | 创建用户 | 创建新用户 |
| `update_user` | 更新用户 | 更新用户信息 |
| `delete_user` | 删除用户 | 删除用户 |
| `view_orders` | 查看订单 | 查看订单列表和详情 |
| `create_order` | 创建订单 | 创建新订单 |
| `update_order` | 更新订单 | 更新订单信息 |
| `delete_order` | 删除订单 | 删除订单 |
| `process_order` | 处理订单 | 处理订单流程 |
| `view_vehicles` | 查看车辆 | 查看车辆列表和详情 |
| `create_vehicle` | 创建车辆 | 创建新车辆 |
| `update_vehicle` | 更新车辆 | 更新车辆信息 |
| `delete_vehicle` | 删除车辆 | 删除车辆 |
| `dispatch_vehicle` | 调度车辆 | 车辆调度管理 |
| `view_finance` | 查看财务 | 查看财务数据 |
| `manage_finance` | 管理财务 | 管理财务数据 |
| `view_settings` | 查看设置 | 查看系统设置 |
| `manage_settings` | 管理设置 | 管理系统设置 |
| `view_hosting` | 查看托管 | 查看托管申请 |
| `review_hosting` | 审核托管 | 审核托管申请 |
| `view_messages` | 查看消息 | 查看消息通知 |
| `manage_tickets` | 管理工单 | 管理工单系统 |
| `chat_support` | 在线客服 | 在线客服支持 |

---

## 🔒 数据权限

### 数据过滤规则

不同角色看到的数据范围不同：

1. **超级管理员** 和 **区域经理**：可以查看所有数据
2. **门店经理**：只能查看自己门店的数据
3. **现场管理员**：只能查看自己处理的数据
4. **客服人员**：只能查看基本信息（敏感信息已脱敏）

### 使用示例

```javascript
import { filterDataByPermission } from '@/utils/permission'

export default {
  data() {
    return {
      allOrders: []
    }
  },

  computed: {
    // 根据用户权限过滤订单数据
    visibleOrders() {
      return filterDataByPermission(this.allOrders, 'orders')
    }
  },

  async loadOrders() {
    // 加载所有订单
    const response = await getOrderList()
    this.allOrders = response.list

    // 数据会自动根据用户角色过滤
    // 门店经理只能看到自己门店的订单
    // 现场管理员只能看到自己处理的订单
  }
}
```

---

## ⚠️ 注意事项

### 1. 用户信息存储

用户登录后，需要将用户信息（包含角色）存储到本地：

```javascript
// 登录成功后
const user = {
  id: 1,
  name: '张三',
  role: 'store_manager', // 角色标识
  storeId: 10 // 门店ID（门店经理需要）
}

uni.setStorageSync('user', JSON.stringify(user))
```

### 2. 权限检查时机

- **页面级权限**：在路由守卫中自动检查
- **功能级权限**：使用权限组件或工具函数检查
- **数据级权限**：使用数据过滤函数处理

### 3. 权限不足处理

当用户权限不足时：
- 页面级：自动跳转到登录页或显示无权限提示
- 功能级：隐藏按钮或显示禁用状态
- 数据级：自动过滤数据

### 4. 开发调试

开发时可以临时修改用户角色进行测试：

```javascript
// 临时设置为超级管理员
const user = JSON.parse(uni.getStorageSync('user'))
user.role = 'super_admin'
uni.setStorageSync('user', JSON.stringify(user))
```

---

## 📊 最佳实践

### 1. 组件级权限控制

优先使用权限组件，代码更清晰：

```vue
<!-- ✅ 推荐 -->
<PermissionButton permission="create_order" @click="handleCreate">
  创建订单
</PermissionButton>

<!-- ❌ 不推荐 -->
<button v-if="hasPermission('create_order')" @click="handleCreate">
  创建订单
</button>
```

### 2. 多权限组合

需要多个权限时，使用数组形式：

```vue
<!-- 满足任一权限即可 -->
<PermissionButton :permissions="['update_order', 'delete_order']">
  操作
</PermissionButton>

<!-- 需要所有权限 -->
<PermissionButton
  :permissions="['view_finance', 'manage_finance']"
  :require-all="true"
>
  财务管理
</PermissionButton>
```

### 3. 数据权限过滤

始终使用数据过滤函数：

```javascript
// ✅ 推荐
computed: {
  visibleOrders() {
    return filterDataByPermission(this.orders, 'orders')
  }
}

// ❌ 不推荐（手动过滤容易出错）
computed: {
  visibleOrders() {
    const role = getCurrentRole()
    if (role === 'store_manager') {
      return this.orders.filter(o => o.storeId === this.user.storeId)
    }
    return this.orders
  }
}
```

---

## 🔍 故障排查

### 问题1：权限检查总是返回false

**原因**：用户信息未正确存储

**解决**：
```javascript
// 检查用户信息
const user = uni.getStorageSync('user')
console.log('用户信息:', user)

// 确保用户信息包含role字段
if (!user || !user.role) {
  console.error('用户信息不完整')
}
```

### 问题2：路由守卫未生效

**原因**：未初始化路由守卫

**解决**：
```javascript
// 在main.js中确保调用了初始化函数
import { initRouteGuard } from '@/utils/route-guard'
initRouteGuard()
```

### 问题3：数据过滤不正确

**原因**：数据结构不符合预期

**解决**：
```javascript
// 确保数据包含必要字段
const order = {
  id: 1,
  storeId: 10,      // 门店ID（门店经理需要）
  assigneeId: 5,    // 处理人ID（现场管理员需要）
  creatorId: 3      // 创建人ID（现场管理员需要）
}
```

---

## 📞 技术支持

如有问题，请联系技术团队或查阅以下文档：
- [权限管理工具类源码](../utils/permission.js)
- [路由守卫源码](../utils/route-guard.js)
- [权限按钮组件源码](../components/common/PermissionButton.vue)
- [权限视图组件源码](../components/common/PermissionView.vue)

---

**文档维护**: 叨叨房车技术团队
**最后更新**: 2025-12-06
**版本**: v1.0.0
