# 移动管理端用户体验优化指南

**文档版本**: v1.0.0 | **创建时间**: 2025-12-06

---

## 📋 目录

1. [新手引导](#新手引导)
2. [功能提示](#功能提示)
3. [操作历史](#操作历史)
4. [交互反馈](#交互反馈)
5. [最佳实践](#最佳实践)

---

## 🎯 新手引导

### 使用 Onboarding 组件

Onboarding 组件提供分步引导功能,帮助新用户快速了解应用功能。

#### 基本使用

```vue
<template>
  <view class="page">
    <!-- 引导组件 -->
    <Onboarding
      ref="onboarding"
      :steps="guideSteps"
      @finish="handleGuideFinish"
      @skip="handleGuideSkip"
    />

    <!-- 页面内容 -->
    <view class="dashboard">
      <view class="stat-card" id="stat-card">统计卡片</view>
      <view class="order-list" id="order-list">订单列表</view>
      <view class="quick-actions" id="quick-actions">快捷操作</view>
    </view>
  </view>
</template>

<script>
import Onboarding from '@/components/common/Onboarding.vue'

export default {
  components: {
    Onboarding
  },

  data() {
    return {
      guideSteps: [
        {
          title: '欢迎使用叨叨房车管理端',
          description: '让我们快速了解一下主要功能',
          target: null,
          position: 'center'
        },
        {
          title: '实时数据统计',
          description: '这里显示今日订单、收入等关键数据',
          target: '#stat-card',
          position: 'bottom'
        },
        {
          title: '订单管理',
          description: '查看和处理所有订单,支持筛选和搜索',
          target: '#order-list',
          position: 'top'
        },
        {
          title: '快捷操作',
          description: '常用功能快速入口,提升工作效率',
          target: '#quick-actions',
          position: 'top'
        }
      ]
    }
  },

  mounted() {
    // 检查是否是首次使用
    const hasShownGuide = uni.getStorageSync('has_shown_guide')
    if (!hasShownGuide) {
      setTimeout(() => {
        this.$refs.onboarding.start()
      }, 500)
    }
  },

  methods: {
    handleGuideFinish() {
      uni.setStorageSync('has_shown_guide', true)
      uni.showToast({
        title: '引导完成',
        icon: 'success'
      })
    },

    handleGuideSkip(stepIndex) {
      uni.setStorageSync('has_shown_guide', true)
      console.log('跳过引导,当前步骤:', stepIndex)
    }
  }
}
</script>
```

#### 引导步骤配置

```javascript
const guideSteps = [
  {
    title: '步骤标题',           // 必填
    description: '步骤描述',     // 必填
    target: '#element-id',       // 目标元素选择器(null表示居中显示)
    position: 'bottom',          // 提示位置: top/bottom/left/right/center
    image: '/static/guide.png'   // 可选:引导图片
  }
]
```

#### 组件属性

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| steps | Array | [] | 引导步骤配置 |
| showSkip | Boolean | true | 是否显示跳过按钮 |
| showArrow | Boolean | true | 是否显示箭头 |
| maskOpacity | Number | 0.7 | 遮罩透明度 |
| highlightPadding | Number | 10 | 高亮区域内边距 |

#### 事件

- `@start` - 引导开始
- `@finish` - 引导完成
- `@skip` - 跳过引导
- `@step-change` - 步骤切换

#### 方法

```javascript
// 开始引导
this.$refs.onboarding.start()

// 重置引导
this.$refs.onboarding.reset()
```

#### 完整示例

```vue
<template>
  <view class="page">
    <Onboarding
      ref="onboarding"
      :steps="guideSteps"
      :show-skip="true"
      :show-arrow="true"
      @start="handleStart"
      @finish="handleFinish"
      @step-change="handleStepChange"
    />

    <view class="content">
      <!-- 页面内容 -->
    </view>

    <!-- 手动触发引导按钮 -->
    <button @tap="showGuide">查看引导</button>
  </view>
</template>

<script>
export default {
  data() {
    return {
      guideSteps: [
        {
          title: '欢迎',
          description: '欢迎使用应用',
          target: null,
          position: 'center'
        },
        {
          title: '功能1',
          description: '这是功能1的说明',
          target: '#feature-1',
          position: 'bottom'
        }
      ]
    }
  },

  methods: {
    showGuide() {
      this.$refs.onboarding.start()
    },

    handleStart() {
      console.log('引导开始')
    },

    handleFinish() {
      uni.setStorageSync('guide_completed', true)
      uni.showToast({
        title: '引导完成',
        icon: 'success'
      })
    },

    handleStepChange(index) {
      console.log('当前步骤:', index)
    }
  }
}
</script>
```

---

## 💡 功能提示

### 使用 FeatureTip 组件

FeatureTip 组件提供轻量级的功能提示,用于突出显示新功能或重要信息。

#### 基本使用

```vue
<template>
  <view class="page">
    <!-- 功能提示 -->
    <FeatureTip
      ref="featureTip"
      title="新功能上线"
      description="现在可以批量处理订单了"
      icon="✨"
      target="#batch-action"
      position="bottom"
      :show-action="true"
      action-text="知道了"
      @action="handleTipAction"
    />

    <!-- 目标元素 -->
    <button id="batch-action" @tap="batchProcess">
      批量处理
    </button>
  </view>
</template>

<script>
import FeatureTip from '@/components/common/FeatureTip.vue'

export default {
  components: {
    FeatureTip
  },

  mounted() {
    // 检查是否已显示过提示
    const hasShownTip = uni.getStorageSync('batch_action_tip_shown')
    if (!hasShownTip) {
      setTimeout(() => {
        this.$refs.featureTip.show()
      }, 1000)
    }
  },

  methods: {
    handleTipAction() {
      uni.setStorageSync('batch_action_tip_shown', true)
    },

    batchProcess() {
      // 批量处理逻辑
    }
  }
}
</script>
```

#### 组件属性

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| title | String | - | 提示标题(必填) |
| description | String | '' | 提示描述 |
| icon | String | '' | 提示图标 |
| target | String | - | 目标元素选择器(必填) |
| position | String | 'bottom' | 提示位置 |
| closable | Boolean | true | 是否可关闭 |
| showMask | Boolean | false | 是否显示遮罩 |
| showAction | Boolean | false | 是否显示操作按钮 |
| actionText | String | '知道了' | 操作按钮文字 |
| autoClose | Number | 0 | 自动关闭时间(毫秒) |

#### 事件

- `@show` - 提示显示
- `@hide` - 提示隐藏
- `@close` - 点击关闭
- `@action` - 点击操作按钮

#### 方法

```javascript
// 显示提示
this.$refs.featureTip.show()

// 隐藏提示
this.$refs.featureTip.hide()
```

#### 使用场景

**1. 新功能提示**

```javascript
// 版本更新后显示新功能提示
const currentVersion = '1.2.0'
const lastVersion = uni.getStorageSync('last_version')

if (lastVersion !== currentVersion) {
  this.$refs.featureTip.show()
  uni.setStorageSync('last_version', currentVersion)
}
```

**2. 操作提示**

```javascript
// 首次使用某功能时显示提示
const hasUsedFeature = uni.getStorageSync('has_used_export')
if (!hasUsedFeature) {
  this.$refs.exportTip.show()
}
```

**3. 重要通知**

```javascript
// 显示重要通知,自动关闭
<FeatureTip
  title="系统维护通知"
  description="今晚22:00-23:00系统维护"
  icon="⚠️"
  :auto-close="5000"
/>
```

---

## 📝 操作历史

### 使用 HistoryManager

HistoryManager 提供操作历史记录功能,支持撤销/重做操作。

#### 基本使用

```javascript
import historyManager from '@/utils/history-manager'

export default {
  data() {
    return {
      canUndo: false,
      canRedo: false
    }
  },

  mounted() {
    // 监听历史状态变化
    this.unsubscribe = historyManager.addListener((state) => {
      this.canUndo = state.canUndo
      this.canRedo = state.canRedo
    })
  },

  beforeDestroy() {
    // 移除监听器
    if (this.unsubscribe) {
      this.unsubscribe()
    }
  },

  methods: {
    async deleteOrder(order) {
      // 保存原始数据
      const originalOrder = { ...order }

      // 执行删除
      await this.$api.deleteOrder(order.id)
      this.orders = this.orders.filter(o => o.id !== order.id)

      // 记录操作
      historyManager.record({
        type: 'delete_order',
        description: `删除订单 ${order.orderNumber}`,
        data: { order: originalOrder },
        undo: async (data) => {
          // 撤销删除:恢复订单
          await this.$api.createOrder(data.order)
          this.orders.push(data.order)
          uni.showToast({ title: '已撤销删除', icon: 'success' })
        },
        redo: async (data) => {
          // 重做删除
          await this.$api.deleteOrder(data.order.id)
          this.orders = this.orders.filter(o => o.id !== data.order.id)
          uni.showToast({ title: '已重新删除', icon: 'success' })
        }
      })
    },

    async handleUndo() {
      const success = await historyManager.undo()
      if (!success) {
        uni.showToast({ title: '无法撤销', icon: 'none' })
      }
    },

    async handleRedo() {
      const success = await historyManager.redo()
      if (!success) {
        uni.showToast({ title: '无法重做', icon: 'none' })
      }
    }
  }
}
```

#### 记录操作

```javascript
historyManager.record({
  type: 'operation_type',        // 操作类型
  description: '操作描述',        // 操作描述
  data: { /* 操作数据 */ },      // 操作相关数据
  undo: async (data) => {        // 撤销函数
    // 撤销逻辑
  },
  redo: async (data) => {        // 重做函数
    // 重做逻辑
  }
})
```

#### 常用方法

```javascript
// 撤销操作
await historyManager.undo()

// 重做操作
await historyManager.redo()

// 检查是否可以撤销/重做
const canUndo = historyManager.canUndo()
const canRedo = historyManager.canRedo()

// 获取历史记录
const history = historyManager.getHistory(20)

// 获取最近操作
const recent = historyManager.getRecentOperations(10)

// 清除历史记录
historyManager.clear()

// 获取统计信息
const stats = historyManager.getStatistics()
```

#### 完整示例:订单管理

```vue
<template>
  <view class="page">
    <!-- 操作按钮 -->
    <view class="toolbar">
      <button :disabled="!canUndo" @tap="handleUndo">撤销</button>
      <button :disabled="!canRedo" @tap="handleRedo">重做</button>
      <button @tap="showHistory">历史记录</button>
    </view>

    <!-- 订单列表 -->
    <view class="order-list">
      <view v-for="order in orders" :key="order.id" class="order-item">
        <text>{{ order.orderNumber }}</text>
        <button @tap="updateOrderStatus(order)">更新状态</button>
        <button @tap="deleteOrder(order)">删除</button>
      </view>
    </view>
  </view>
</template>

<script>
import historyManager from '@/utils/history-manager'

export default {
  data() {
    return {
      orders: [],
      canUndo: false,
      canRedo: false
    }
  },

  mounted() {
    this.loadOrders()

    // 监听历史状态
    this.unsubscribe = historyManager.addListener((state) => {
      this.canUndo = state.canUndo
      this.canRedo = state.canRedo
    })
  },

  beforeDestroy() {
    if (this.unsubscribe) {
      this.unsubscribe()
    }
  },

  methods: {
    async loadOrders() {
      const res = await this.$api.getOrderList()
      this.orders = res.list
    },

    async updateOrderStatus(order) {
      const oldStatus = order.status
      const newStatus = 'confirmed'

      // 更新状态
      order.status = newStatus
      await this.$api.updateOrder(order.id, { status: newStatus })

      // 记录操作
      historyManager.record({
        type: 'update_order_status',
        description: `更新订单 ${order.orderNumber} 状态`,
        data: { orderId: order.id, oldStatus, newStatus },
        undo: async (data) => {
          const order = this.orders.find(o => o.id === data.orderId)
          if (order) {
            order.status = data.oldStatus
            await this.$api.updateOrder(data.orderId, { status: data.oldStatus })
          }
        },
        redo: async (data) => {
          const order = this.orders.find(o => o.id === data.orderId)
          if (order) {
            order.status = data.newStatus
            await this.$api.updateOrder(data.orderId, { status: data.newStatus })
          }
        }
      })
    },

    async deleteOrder(order) {
      const index = this.orders.indexOf(order)

      // 删除订单
      this.orders.splice(index, 1)
      await this.$api.deleteOrder(order.id)

      // 记录操作
      historyManager.record({
        type: 'delete_order',
        description: `删除订单 ${order.orderNumber}`,
        data: { order, index },
        undo: async (data) => {
          this.orders.splice(data.index, 0, data.order)
          await this.$api.createOrder(data.order)
        },
        redo: async (data) => {
          const idx = this.orders.findIndex(o => o.id === data.order.id)
          if (idx > -1) {
            this.orders.splice(idx, 1)
            await this.$api.deleteOrder(data.order.id)
          }
        }
      })
    },

    async handleUndo() {
      await historyManager.undo()
    },

    async handleRedo() {
      await historyManager.redo()
    },

    showHistory() {
      const history = historyManager.getHistory(20)
      console.log('操作历史:', history)
      // 显示历史记录弹窗
    }
  }
}
</script>
```

---

## 🎨 交互反馈

### 加载状态

```vue
<template>
  <view class="page">
    <!-- 全局加载 -->
    <view v-if="loading" class="loading-overlay">
      <view class="loading-spinner"></view>
      <text class="loading-text">加载中...</text>
    </view>

    <!-- 按钮加载 -->
    <button :loading="submitting" @tap="handleSubmit">
      提交
    </button>

    <!-- 列表加载 -->
    <view v-if="listLoading" class="list-loading">
      <text class="loading-icon">⏳</text>
      <text>加载中...</text>
    </view>
  </view>
</template>
```

### 操作反馈

```javascript
// 成功提示
uni.showToast({
  title: '操作成功',
  icon: 'success',
  duration: 2000
})

// 失败提示
uni.showToast({
  title: '操作失败',
  icon: 'error',
  duration: 2000
})

// 警告提示
uni.showToast({
  title: '请先选择订单',
  icon: 'none',
  duration: 2000
})

// 确认对话框
uni.showModal({
  title: '确认删除',
  content: '删除后无法恢复,确定要删除吗?',
  success: (res) => {
    if (res.confirm) {
      this.deleteItem()
    }
  }
})
```

### 震动反馈

```javascript
// 轻震动
uni.vibrateShort({
  type: 'light'
})

// 中等震动
uni.vibrateShort({
  type: 'medium'
})

// 重震动
uni.vibrateShort({
  type: 'heavy'
})

// 长震动
uni.vibrateLong()
```

---

## 🎯 最佳实践

### 1. 首次使用引导

```javascript
// 检查是否首次使用
const isFirstTime = !uni.getStorageSync('app_initialized')

if (isFirstTime) {
  // 显示欢迎页面
  uni.navigateTo({
    url: '/pages/welcome/index'
  })

  // 标记已初始化
  uni.setStorageSync('app_initialized', true)
}
```

### 2. 版本更新提示

```javascript
// 检查版本更新
const currentVersion = '1.2.0'
const lastVersion = uni.getStorageSync('last_version')

if (lastVersion && lastVersion !== currentVersion) {
  // 显示更新日志
  uni.showModal({
    title: '版本更新',
    content: '新版本已上线,查看更新内容?',
    success: (res) => {
      if (res.confirm) {
        uni.navigateTo({
          url: '/pages/changelog/index'
        })
      }
    }
  })
}

uni.setStorageSync('last_version', currentVersion)
```

### 3. 功能使用统计

```javascript
// 记录功能使用次数
function trackFeatureUsage(featureName) {
  const key = `feature_usage_${featureName}`
  const count = uni.getStorageSync(key) || 0
  uni.setStorageSync(key, count + 1)

  // 首次使用显示提示
  if (count === 0) {
    showFeatureTip(featureName)
  }
}

// 使用
trackFeatureUsage('batch_export')
```

### 4. 操作确认

```javascript
// 危险操作二次确认
async function confirmDangerousAction(message) {
  return new Promise((resolve) => {
    uni.showModal({
      title: '确认操作',
      content: message,
      confirmText: '确定',
      confirmColor: '#f56c6c',
      success: (res) => {
        resolve(res.confirm)
      }
    })
  })
}

// 使用
const confirmed = await confirmDangerousAction('删除后无法恢复,确定要删除吗?')
if (confirmed) {
  await deleteItem()
}
```

### 5. 错误处理

```javascript
// 统一错误处理
function handleError(error, context = '') {
  console.error(`Error in ${context}:`, error)

  let message = '操作失败'

  if (error.message) {
    message = error.message
  } else if (error.errMsg) {
    if (error.errMsg.includes('timeout')) {
      message = '请求超时,请重试'
    } else if (error.errMsg.includes('fail')) {
      message = '网络错误,请检查网络连接'
    }
  }

  uni.showToast({
    title: message,
    icon: 'none',
    duration: 3000
  })
}

// 使用
try {
  await this.loadData()
} catch (error) {
  handleError(error, 'loadData')
}
```

---

## 📊 用户体验检查清单

### 新用户体验

- [ ] 提供首次使用引导
- [ ] 关键功能有提示说明
- [ ] 空状态有友好提示
- [ ] 错误信息清晰易懂

### 操作反馈

- [ ] 所有操作有加载状态
- [ ] 成功/失败有明确提示
- [ ] 危险操作有二次确认
- [ ] 支持撤销重要操作

### 性能体验

- [ ] 页面加载流畅
- [ ] 列表滚动不卡顿
- [ ] 图片加载优化
- [ ] 网络请求有超时处理

### 交互体验

- [ ] 按钮点击有反馈
- [ ] 表单验证及时
- [ ] 手势操作流畅
- [ ] 适配深色模式

---

**文档维护**: 叨叨房车技术团队
**最后更新**: 2025-12-06
**版本**: v1.0.0
